# Default: each user supplies their own dCloud token (Chrome import or paste in Step 1).
# Set DCLOUD_ALLOW_ENV_AUTH=1 only on your own machine for dev/automation shortcuts.

# Optional dev-only shortcuts (ignored unless DCLOUD_ALLOW_ENV_AUTH=1)
DCLOUD_TOKEN=
DCLOUD_USERNAME=
DCLOUD_PASSWORD=
DCLOUD_BASIC_TOKEN=

PORT=8768
