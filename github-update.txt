# GitHub source used by start.command. HTTPS, no SSH keys, no tokens in this file.
# Coworkers do not log into this repo if it is public.
repo=jasmurra/dcloud-content-manager
branch=main
# Anonymous unique-install pings (hashed random id + version only).
usage_topic=dcloud-cm-usage-a8f31c9e
